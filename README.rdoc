Purpose of breakout 
  
  - Create, edit, and view tasks from a single page
  

1) git clone git@github.com:FrankOrtiz/Wyncode-Ajax.git     (clone repo)

2) rake db:migrate      (database migrate)

3) rails s      (run server)
